# Use Next.js, Lexical, and SQLite

The demo will be built as a Next.js App Router application using React Lexical for the editor and SQLite for local persistence. This keeps the UI, OAuth callbacks, Graph operations, DOCX conversion, and **終了取り込み** APIs in one deployable demo app without introducing separate SPA and API projects.
