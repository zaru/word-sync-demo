# Use Pandoc for Markdown and DOCX conversion

The demo will use Pandoc on the backend for Markdown-to-DOCX export and DOCX-to-Markdown import. This introduces a local binary dependency, but avoids building custom conversion logic and keeps the project focused on OneDrive-mediated Word editing.
